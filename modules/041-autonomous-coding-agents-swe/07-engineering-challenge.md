# Engineering Challenge

Build a **Self-Healing Python Refactoring Agent**.
Requirements:
1. Parse a Python file into an AST.
2. Identify a specific code smell (e.g. unused imports).
3. Automatically patch the AST.
4. Unparse back to code and run the test suite to verify no regressions.
