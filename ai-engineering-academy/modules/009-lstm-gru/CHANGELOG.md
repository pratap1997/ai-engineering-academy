# Module 009 — Version History

## [1.0.0] - 2026-08-01

### Added
- Created complete 9-artifact curriculum for Advanced Recurrent Architectures (LSTM & GRU).
- Pure Python & NumPy implementations of LSTMCell, LSTMSequence, GRUCell, and GRUSequence (`04-implementation.py`).
- Experiments covering 100-step long-term dependency retrieval (Vanilla RNN vs LSTM) and GRU speed/parameter comparison (`05-experiments.py`).
- Custom LSTM sequence challenge & solution script with gradcheck verification (`07-challenge-solution.py`).
- Pytest test suite with 16 tests (`tests/test_lstm_gru.py`).
- Standalone interactive HTML visualizer displaying live cell state conveyor belt flow and gate activations (`assets/lstm-standalone.html`).
